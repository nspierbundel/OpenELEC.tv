RunScript(script.tlbb.systemsettings,setLanguage)

RunScript(script.tlbb.systemsettings,SetSubtitleDownload)

RunScript(script.tlbb.systemsettings,SetSubtitlePreferred)

RunScript(script.tlbb.systemsettings,SetSubtitleCharset)

RunScript(script.tlbb.systemsettings,SetRegion)

RunScript(script.tlbb.systemsettings,zoomUp)

RunScript(script.tlbb.systemsettings,zoomDown)

RunScript(script.tlbb.systemsettings,setTimezoneCountry)

RunScript(script.tlbb.systemsettings,setTimezone)


RunScript(script.tlbb.systemsettings,refresh)
    this will set the following skin strings
        firmware

        locale.language
        locale.country
        locale.subtitlelanguage    
        locale.timezonecountry
        locale.timezone

        lookandfeel.skinzoom
        lookandfeel.skin

        subtitles.languages
        subtitles.charset - Displayed text not the character set, eg 'ISO-8859-1' would appear as 'Western Europe (ISO)'
        

Skin Bools
LanguageSet
SubtitleDownloadSet
SubtitlePreferredSet
SubtitleCharsetSet
RegionSet
TimezoneCountrySet
TimezoneSet
